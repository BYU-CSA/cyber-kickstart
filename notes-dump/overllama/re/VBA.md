If you have to rev a macro, use `olevba`, which can be installed with `pip3 install oletools`.
```sh
olevba file.xls -c
```
The `-c` flag makes it just analyze and return the decompiled code, nothing more